Nicer reservation lines that don't jump around.

Multiple paralel one-way parking spots.

Profile code
Remove excessive comments from code.

Maybe make the savegame ID much higher, so it doesn't collide with new upstream versions. Check what other patch packs do.
