* Stock heliports as modular don't use the right pad types (used fixed templates instead)

When tile is deleted and aircraft moved to hangar: If in flight it should be taken out of flight. Otherwise stuck in hangar.

